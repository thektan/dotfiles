#!/usr/bin/env bash

############################################################
# Environment Variables                                    #
############################################################

# Defining hostname for liferay properties file
export HOSTNAME=$(hostname)

# Setting ANT_OPS variable for ant command
export ANT_OPTS="-Xms2048m -Xmx4096m -XX:MaxPermSize=2048m"

############################################################
# Java                                                     #
############################################################

export JAVA_8_HOME=$(/usr/libexec/java_home -v1.8)
# export JAVA_7_HOME=$(/usr/libexec/java_home -v1.7)

# Default java8
export JAVA_HOME=$JAVA_8_HOME

############################################################
# Exports
############################################################

# Directories
MASTER=~/Liferay/master/portal

############################################################
# Functions
############################################################

# Find gradle in the root of git directory and execute it.
# Otherwise use globally installed gradle.
# @author Nate Cavanaugh
gradle() {
	local root_level=$(git rev-parse --show-toplevel 2>/dev/null)

	if [[ -n "$root_level" && -f "$root_level/gradlew" ]];
		then root_level="$root_level/gradlew"
	else
		root_level=$(which gradle)
fi

	"$root_level" $@
}

# Get the last mentioned JIRA ticket id from the commit log
# @author Nate Cavanaugh
latest_jira_ticket() {
	git log $1 --oneline | grep -Eo '([A-Z]{2,}-)([0-9]+)' -m 1;
}

# Open the url to the latest JIRA ticket referenced in the log
open_jira_ticket() {
	if [ -z "$1" ]; then
		latestTicket=$(latest_jira_ticket);
		open http://issues.liferay.com/browse/$latestTicket;
	fi

	if [ -n "$1" ]; then
		open http://issues.liferay.com/browse/$1;
	fi
}

# Gets the JIRA url for the latest JIRA ticket referenced in the log
latest_jira_ticket_url() {
	export latestTicketURL="http://issues.liferay.com/browse/$(latest_jira_ticket)";
}

# Submit a pull request
# $1 branch name - the branch to send the pull request to
# $2 username - pull request recipient
# $3 string - extra descriptions
submit_pull_request() {
	if [ -n "$1" ] && [ -n "$2" ]; then
		open_jira_ticket;

		description="";

		if [ -n "$3" ]; then
			description="<br><br>$3";
		fi

		gh pr --branch $1 --submit $2 -D "http://issues.liferay.com/browse/$(latest_jira_ticket) $description";
	fi
}

# Kill tomcat server
kt() {
	if [ -n "$1" ]; then
		local branchName="master"

		if [[ "$1" == "6" ]]; then
			branchName="ee-6.2.x"
		elif [[ "$1" == "7" ]]; then
			branchName="ee-7.0.x"
		elif [[ "$1" == "f" ]]; then
			branchName="ee-7.0.x-faro"
		elif [[ "$1" == "m" ]]; then
			branchName="master"
		else
			echo "Accepted values: [ 6 | 7 | f | m ]";
		fi

		echo "Killing process for branch: $branchName\n"

		pkill -9 -f $branchName/bundles/tomcat;
	else
		echo "Usage: kt [ 6 | 7 | f | m ]\n"
	fi
}

############################################################
# Gradle
############################################################
alias grcd1="../gradlew clean deploy"
alias grcd2="../../gradlew clean deploy"
alias grcd3="../../../gradlew clean deploy"
alias grcd4="../../../../gradlew clean deploy"
alias grcd5="../../../../../gradlew clean deploy"

############################################################
# Node GH
############################################################

alias ghp="gh pr"

alias ghb="gh pr --browser"
alias ghc="gh pr --comment"
alias ghi="gh pr --info"

alias ghs="submit_pull_request"
alias ghsm="submit_pull_request master"
alias ghsme="gh pr --submit thektan --title ''"

############################################################
# Liferay
############################################################

# Check all source formatting in current directories
alias csf="find . | xargs check_sf -q"
alias sf="gradlew formatSource"

# Master - Portal
alias lrmp="cd $MASTER"
alias lrmpa="atom $MASTER"
alias lrmps="subl $MASTER"
alias lrmpf="open $MASTER"

############################################################
# Faro
############################################################

FARO=~/Liferay/ee-7.0.x-faro/portal-ee/modules/private/apps/osb-faro
FARO_WEB=~/Liferay/ee-7.0.x-faro/portal-ee/modules/private/apps/osb-faro/osb-faro-web
FARO_FUNCTIONAL_TEST=~/Liferay/ee-7.0.x-faro/portal-ee/modules/private/apps/osb-faro/osb-faro-functional-test

alias farou="git checkout 7.0.x-private"
alias faroup="git checkout 7.0.x-private && git pull upstream 7.0.x-private && git push origin 7.0.x-private"
alias faroda="cd $FARO && ../../../../gradlew clean deploy && cd -"
alias farot="cd $FARO && ./gradlew npmRunFormat && ./gradlew npmRunTest && ./gradlew formatSource && cd -"
alias faroti="cd $FARO_FUNCTIONAL_TEST && ../gradlew testIntegration && cd -"

alias farod="cd $FARO_WEB && ../gradlew clean deploy && cd -"
alias farons="cd $FARO_WEB && ../gradlew npmRunStart && cd -"
alias faros="cd $FARO && ./gradlew startDocker && docker logs --follow com-liferay-osb-faro-private"
alias farow="cd $FARO && docker logs --follow com-liferay-osb-faro-private"

############################################################
# Search
############################################################

# Update the custom rankings dev branch
search_update_dev() {
	git checkout custom_rankings;
	git fetch bryan custom_rankings;
	git reset --hard bryan/custom_rankings;
	git push origin custom_rankings --force;
	git checkout -;
}

alias searchupdatedev=search_update_dev;

############################################################
# Tomcat
############################################################

# Start Tomcat server
alias stomcat="./catalina.sh run"
# Master
alias lrsm="cd ~/Liferay/master/bundles/tomcat/bin && ./catalina.sh run"
# Faro
alias lrsf="mysql.server start && cd ~/Liferay/ee-7.0.x-faro/bundles/tomcat/bin && ./catalina.sh run"

# Find specific processes
alias pj="ps -ef | ag java"
alias pt="ps -ef | ag bundles/tomcat/"

alias ptm='ps -ef | ag master/bundles/tomcat/ | ag java | awk '\''{print $2}'\'''

# Kill processes
alias km="kill -9 $(ptm)"

# Kill and restart servers
alias kmr="kill -9 $(ptm) && lrsm"

alias killp="kill -9"

############################################################
# Misc
############################################################

# Jira
alias jo=open_jira_ticket;